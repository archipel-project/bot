name: Build project

on:
  push:
    tags:
      - '*'

jobs:
  build-project:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup
        uses: gradle/gradle-build-action@v2

      - name: Build
        run: ./gradlew build